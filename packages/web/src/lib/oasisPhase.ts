// Thin re-export — the canonical implementation lives in @salvador/shared
// (see packages/shared/src/oasis/phase.ts). Kept here to avoid churn in
// existing import sites.
export { OASIS_PHASES_IN_ORDER, phaseFromTagId } from "@salvador/shared";
